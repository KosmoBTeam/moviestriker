
create table member(
num number(5)constraint member_num_pk primary key,
mailphone varchar2(40) constraint member_email_uk unique,
pwd varchar2(30),
name varchar2(30),
mdate date
);

create sequence member_seq
start with 1
increment by 1;